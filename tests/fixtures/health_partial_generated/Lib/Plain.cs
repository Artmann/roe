namespace Lib;

// Nothing generated anywhere near this one, so it is the control: if it is
// not reported, the thresholds were wrong rather than the filtering right.
public class Plain
{
    public int First;
    public int Second;
    public int Third;
    public int Fourth;
}
