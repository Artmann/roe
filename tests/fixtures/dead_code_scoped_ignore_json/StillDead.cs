namespace Messaging;

// Not covered by any ignore glob, so this finding must survive the
// `deadCode.ignore` filtering that drops Handlers/.
internal class StillDead
{
    public void DoNothing()
    {
    }
}
